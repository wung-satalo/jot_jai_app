import 'package:flutter/material.dart';

class AppColors {
  // 🌊 Primary Colors
  static const primary = Color(0xFF395886);
  static const primaryLight = Color(0xFF638ECB);
  static const accent = Color(0xFF8AAEE0);

  // 🧱 Backgrounds
  static const background = Color(0xFFF0F3FA);
  static const surface = Color(0xFFFFFFFF);
  static const softBackground = Color(0xFFD5DEEF);

  // 📏 Borders / Divider
  static const border = Color(0xFFB1C9EF);
  static const reward = Color(0xFFFFA02E);
  static const amber900 = Color(0xFFFF6F00);

  // ✍️ Text Colors
  static const textPrimary = Color(0xFF1F2A44);
  static const textSecondary = Color(0xFF6B7A99);
  static const textOnPrimary = Colors.white;

  // 🌙 Dark Mode (optional)
  static const darkBackground = Color(0xFF0F172A);
  static const darkSurface = Color(0xFF1E293B);
}